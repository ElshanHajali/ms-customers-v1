spring:
  application:
    name: apigw
  cloud:
    gateway:
      routes:
        - id: customers
          uri: lb://CUSTOMERS
          predicates:
            - Path=/v1/customers/**
        - id: fraud
          uri: lb://FRAUD
          predicates:
            - Path=/v1/fraud-check/**

server:
  port: 9090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true

